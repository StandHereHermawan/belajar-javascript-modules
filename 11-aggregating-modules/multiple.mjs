/**
 * Export Multiple
 */
const company = "Belum Ada .Tbk";

function sum(first, second) {
    return first + second;
}

class Company {
    constructor(lokasi, nama) {
        this.lokasi = lokasi;
        this.nama = nama;
    }
}

export { company, sum, Company };
